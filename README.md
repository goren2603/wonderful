# wonderful-demo

## Status

This repository is currently empty — it's set up as a clean starting point for a new project. No app code has been written yet.

## Setup

Once a tech stack is chosen, replace this section with concrete steps. Typical pattern:

```bash
git clone <this-repo-url>
cd wonderful-demo
```

Then, depending on the stack chosen for this project:

- **Node.js**: `npm install` (or `yarn` / `pnpm install`), then `npm run dev`
- **Python**: create a virtual environment (`python -m venv .venv`), activate it, then `pip install -r requirements.txt`

## Contributing

- Create a feature branch off `main` for your changes.
- Keep commits focused and descriptive.
- Do not commit secrets, credentials, or `.env` files — see [.gitignore](.gitignore).

## Working with OpenAI Codex

This project is set up to be opened directly in the Codex app.

### Opening this folder in Codex on Windows

1. Install the Codex app (or the Codex CLI/IDE extension you use) if you haven't already.
2. Open the Codex app.
3. Choose **Open Folder** (or **Open Project**) from the Codex app's start screen or File menu.
4. Navigate to and select this folder:
   ```
   C:\wonderful.ai\wonderful-demo
   ```
5. Confirm/open — Codex will load the repository at that path and pick up this `README.md` and `.gitignore` automatically.

If your Codex app instead prompts for a Git remote URL rather than a local folder, push this repository to your Git host (e.g. GitHub) first, then provide that URL when opening the project in Codex.
